name: Deploy on Notion Update

on:
  repository_dispatch:
    types: [notion-content-updated]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Verify environment variables
        run: |
          if [ -z "$NOTION_TOKEN" ]; then
            echo "ERROR: NOTION_TOKEN is not set"
            exit 1
          fi
          if [ -z "$NOTION_DATABASE_ID" ]; then
            echo "ERROR: NOTION_DATABASE_ID is not set"
            exit 1
          fi
          echo "NOTION_TOKEN is set (length: ${#NOTION_TOKEN})"
          echo "NOTION_DATABASE_ID is set: $NOTION_DATABASE_ID"
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}

      - name: Build
        env:
          NOTION_TOKEN: ${{ secrets.NOTION_TOKEN }}
          NOTION_DATABASE_ID: ${{ secrets.NOTION_DATABASE_ID }}
        run: npm run build

      - name: Deploy to Cloudflare Pages
        run: |
          echo "Checking environment variables..."
          if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "ERROR: CLOUDFLARE_API_TOKEN is not set"
            exit 1
          fi
          if [ -z "$CLOUDFLARE_ACCOUNT_ID" ]; then
            echo "ERROR: CLOUDFLARE_ACCOUNT_ID is not set"
            exit 1
          fi
          echo "Environment variables are set"
          npx wrangler pages deploy out --project-name=tokechan-blog
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CF_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          CF_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
